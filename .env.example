# AliaProxy 环境变量配置示例
# 复制此文件为 .env 并根据需要修改

# ==================== 数据库配置 ====================
# 支持 SQLite（默认）、PostgreSQL、MySQL
# SQLite 格式: sqlite:///绝对路径 或 sqlite://相对路径
# PostgreSQL 格式: postgres://user:password@host:port/database
# MySQL 格式: mysql://user:password@host:port/database
ALIA_DATABASE_URL=sqlite:///data/alia.db

# 可选：PostgreSQL 密码（如果使用 docker-compose 中的 postgres 服务）
# POSTGRES_PASSWORD=your_secure_password_here

# ==================== 存储配置 ====================
# 媒体文件存储目录（容器中路径）
ALIA_MEDIA_DIR=/app/data/alia-media

# ==================== 应用配置 ====================
# 调试模式（true/false）
ALIA_DEBUG=false

# 配置热重载（true/false）
# 启用后，修改 config.toml 会自动重新加载配置
ALIA_HOT_RELOAD=false

# ==================== Docker Compose 配置 ====================
# 如需使用 PostgreSQL 而非 SQLite，取消以下注释并注释掉 SQLite 配置
# ALIA_DATABASE_URL=postgres://alia:changeme@postgres:5432/alia
