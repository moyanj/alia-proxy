version: '3.8'

services:
  aiprox:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    ports:
      - "8000:8000"
    volumes:
      # 数据持久化（SQLite 和媒体文件）
      - ./data:/home/moyan/projects/aiprox/data
      # 配置文件（可选：如果需要运行时修改配置）
      - ./config.toml:/home/moyan/projects/aiprox/config.toml
    environment:
      # 自定义数据库连接字符串（默认使用 SQLite）
      # 支持 SQLite、PostgreSQL、MySQL 等
      # SQLite 示例: sqlite:///data/aiprox.db
      # PostgreSQL 示例: postgres://user:pass@localhost:5432/aiprox
      - AIPROX_DATABASE_URL=${AIPROX_DATABASE_URL:-sqlite:///data/aiprox.db}
      - AIPROX_MEDIA_DIR=${AIPROX_MEDIA_DIR:-/home/moyan/projects/aiprox/data/media}
      # 启用调试模式（可选）
      - AIPROX_DEBUG=${AIPROX_DEBUG:-false}
      # 启用配置热重载（可选）
      - AIPROX_HOT_RELOAD=${AIPROX_HOT_RELOAD:-false}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped

  # 可选：PostgreSQL 数据库服务（如果需要使用 PostgreSQL 而非 SQLite）
  # 取消注释以下部分以启用 PostgreSQL
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: aiprox
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
  #     POSTGRES_DB: aiprox
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   restart: unless-stopped

# volumes:
#   postgres_data:
